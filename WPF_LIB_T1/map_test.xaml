<UserControl x:Class="WPF_LIB_T1.map_test"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_LIB_T1"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" x:Name="x1">
    <Grid >

        <StackPanel  Orientation="Horizontal" x:Name="SP">
            <ItemsControl ItemsSource="{Binding}">

            </ItemsControl>
        </StackPanel>
        <StackPanel x:Name="SP2" Orientation="Horizontal">
            <ItemsControl ItemsSource="{Binding}">

            </ItemsControl>
        </StackPanel>
        <Button Content="Button" HorizontalAlignment="Left" Margin="565,215,0,0" VerticalAlignment="Top" Width="75" Click="Button_Click"/>
    </Grid>

</UserControl>
